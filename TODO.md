1. <strike>Accept negative values `from`, `to`.
    - Treat `to=0` as now.
    - Check if `from<to`.</strike>
    
2. <strike>Add time information to Trend class.</strike>
3. <strike>Accept trendId.</strike>
4. <strike>Try different options in RF model.</strike>